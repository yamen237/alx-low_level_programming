#include "main.h"
#include <string.h>
/**
 * _strlen - return the length of a string
 * @s: take in string by pointeur
 *int  _strlen: return the length of srting
 * Return: return the length of string
 */
int _strlen(char *s)
{
return (strlen(s));
}
