#include <stdio.h>
/**
 * main - Entry point
 * Return: Always 0 (Success)
 */
int main(void)
{
	for(char alphabet = 'a'; alphabet <= 'z'; alpabet++)
		putchar(alphabet);
	putchar("\n");
	return (0);
}
